#include <stdio.h>

int main() {

    int numero;  // Declaração
    numero = 10; // Atribuição

    char letra;
    letra = 'A';

    float real;
    real = 3.45;

    int idade = 12; // Inicialização de variável

    int n1, n2, n3, n4; // Inicialização múltipla

    /*
    Deve começar com uma letra
    Os próximos podem ser letras ou números
    Não pode utilizar nenhum símbolo, exceto _
    Não pode conter espaços em branco
    Não pode conter letras com acentos
    Não pode ser uma palavra reservada
    Escolher bons nomes
    */

    return 0;
}